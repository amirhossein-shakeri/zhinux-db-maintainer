services:
  postgres:
    environment:
      POSTGRES_USER: dev
      POSTGRES_PASSWORD: dev
      POSTGRES_DB: app

  db-tools:
    image: postgres:18-alpine
    profiles: ["tools"]
    working_dir: /workspace
    volumes:
      - .:/workspace
